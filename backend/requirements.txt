# SyncForge backend — CORE deps (ships in .exe bundle)
fastapi>=0.115
uvicorn[standard]>=0.34
websockets>=13
pydantic>=2.9
python-dotenv>=1.0

# TTS
edge-tts>=7.0

# LLMs
google-genai>=0.8
groq>=0.11
openai>=1.50

# Stock / video
yt-dlp>=2025.1
requests>=2.32
beautifulsoup4>=4.12

# Image / pHash dedup
pillow>=10.4
imagehash>=4.3
numpy>=2.0

# Optional ML deps (install via requirements-ml.txt only if you need
# SigLIP-2 local embeddings or Whisper local transcription):
#   pip install -r backend/requirements-ml.txt
